steps:
  - name: ":hammer:"
    plugins:
      golang-build#1a1e0cd:
        build: main.go
        package: github.com/lox/parfait
        targets:
         - version: 1.10.0
           goos: linux
           goarch: amd64

  - wait

  - name: ":github: Release"
    command: .buildkite/release.sh

